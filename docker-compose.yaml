version: '3'

services:
  auth-generator:
    image: andrianovartemii/gm-test-task-auth-generator:amd
    ports:
      - "8082:8080"
    environment:
      AUTH_TOKEN_LIFETIME: 10
    platform: linux/arm64/v8